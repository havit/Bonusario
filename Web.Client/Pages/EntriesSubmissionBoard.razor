@attribute [Route(Routes.SubmissionBoard)]

<div class="mb-3"><PeriodPicker @bind-Value="periodId" Nullable="false" /></div>

@if (periodId is not null)
{
	<EntriesBoard PeriodId="periodId.Value" />
}